#!/bin/bash

set -e

MDLH_ARGS="\
  --device-type virtual \
  --platform linux/amd64 \
  --device-group mender-docker-lifecycle-helper-example \
  $(pwd)/examples/${1}/docker-compose.yaml \
  ${*:2} \
"
export MDLH_ARGS

bash "$( dirname -- "$( dirname -- "${BASH_SOURCE[0]}" )")/mender-docker-lifecycle-helper-launcher"
